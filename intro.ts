// we create .ts file and write ts code
// but if we run that file using tsc intro.ts then it transpiled into intro.js
// it means, ts is a just a development tool actually all code runs in js

let user = {
    name: 'seemant',
    age: 20,
    email: 'seemant@gmail.com'
}

console.log(user.name);